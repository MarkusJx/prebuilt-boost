name: Build Boost

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Boost version to build'
        required: true

jobs:
  build:
    strategy:
      matrix:
        os: ['ubuntu-latest', 'windows-latest', 'macOs-latest']

    runs-on: ${{matrix.os}}
    steps:
      - name: ls
        run: ls
